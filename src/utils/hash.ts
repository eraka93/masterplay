/** A small, non-cryptographic string hash — good enough for deterministic UI seeding and doc IDs. */
export function hashString(value: string): number {
  let hash = 0
  for (let i = 0; i < value.length; i += 1) {
    hash = (hash * 31 + value.charCodeAt(i)) >>> 0
  }
  return hash
}
